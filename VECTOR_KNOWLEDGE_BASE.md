# 智能知识库系统

## 🎯 功能概述

全新的向量数据库驱动的知识库系统，实现了真正的智能文档检索：

- **自动检索**: AI会根据用户问题自动从知识库中找到相关文档
- **智能匹配**: 基于语义相似度而非关键字匹配
- **无缝集成**: 检索结果自动融入对话上下文
- **多格式支持**: 支持文本、PDF、Word、Excel等格式

## 🔧 技术架构

### 核心组件

1. **SimpleVectorDB** (`services/vectorDB/simpleVectorDB.ts`)
   - 轻量级内存向量数据库
   - 基于文本相似度算法
   - 自动文档分块处理

2. **KnowledgeRetrievalService** (`services/knowledgeRetrieval/index.ts`)
   - 智能检索服务
   - 上下文生成
   - 相关性判断

3. **集成到聊天流程** (`services/processQuery.ts`)
   - 自动知识库检索
   - 上下文注入
   - 透明的用户体验

### 工作流程

```
用户提问 → 相关性判断 → 向量检索 → 上下文生成 → AI回答
```

## 📋 功能特性

### ✅ 已实现
- **文档上传**: 自动向量化存储
- **智能检索**: 基于相似度的文档检索
- **自动集成**: 检索结果自动加入对话上下文
- **多格式支持**: 文本格式完全支持，其他格式显示文件信息
- **实时同步**: 文档增删改实时同步向量数据库

### 🔄 工作原理

1. **文档上传时**:
   ```typescript
   文件 → 内容解析 → 自动分块 → 向量化存储 → 数据库保存
   ```

2. **用户对话时**:
   ```typescript
   用户问题 → 相关性判断 → 向量检索 → 上下文注入 → AI回答
   ```

3. **智能判断**:
   - 根据问题内容判断是否需要知识库
   - 包含查询、解释、说明等关键词时自动启用
   - 长问题更可能使用知识库支持

## 🚀 使用方法

### 1. 上传文档
- 在知识库页面点击"上传文件"
- 支持格式：`.txt`, `.md`, `.json`, `.csv`, `.xml`, `.html`, `.pdf`, `.doc`, `.docx`, `.xls`, `.xlsx`
- 文档自动分块并向量化存储

### 2. 智能对话
- 正常提问，系统会自动判断是否需要知识库支持
- 相关文档内容会自动融入AI回答中
- 无需手动选择文档

### 3. 手动检索
- 使用API端点 `POST /api/knowledge/search` 进行手动检索
- 参数：`query`（查询内容）、`maxResults`（最大结果数）、`threshold`（相似度阈值）

## 🛠️ API接口

### 智能检索
```bash
POST /api/knowledge/search
Content-Type: application/json

{
  "query": "如何配置数据库",
  "maxResults": 5,
  "threshold": 0.6
}
```

### 统计信息
```bash
GET /api/knowledge/stats
```

返回：
```json
{
  "success": true,
  "data": {
    "totalDocuments": 10,
    "totalChunks": 45,
    "isAvailable": true
  }
}
```

## ⚙️ 配置参数

### 检索参数
- **相似度阈值**: 默认 0.3（范围 0-1，越高越严格）
- **最大结果数**: 默认 5 个文档片段
- **文档分块大小**: 1000 字符
- **上下文长度限制**: 3000 字符

### 启发式规则
系统会在以下情况自动使用知识库：
- 包含查询相关关键词：`查询`, `搜索`, `找`, `什么是`, `如何`, `怎么`, `解释`, `说明`等
- 问题长度超过10个字符
- 知识库中有可用文档

## 🔄 升级路径

当前使用的是简单文本相似度算法。未来可以升级到：

1. **真正的向量嵌入**: 使用 OpenAI Embeddings 或本地模型
2. **ChromaDB集成**: 专业的向量数据库
3. **语义搜索**: 更精确的语义理解
4. **混合检索**: 关键词 + 向量的混合搜索

## 🐛 故障排除

### 常见问题

1. **向量数据库初始化失败**
   - 检查日志中的错误信息
   - 系统会自动降级到基本模式

2. **检索结果不准确**
   - 调整相似度阈值（降低阈值获取更多结果）
   - 优化文档内容质量

3. **文档内容为空**
   - 检查文件格式是否支持
   - 查看上传时的错误信息

### 日志监控
```bash
# 查看向量数据库相关日志
grep "向量数据库" logs/app.log

# 查看知识库检索日志
grep "知识库检索" logs/app.log
```

## 📈 性能特点

- **内存使用**: 轻量级，所有数据存储在内存中
- **查询速度**: 毫秒级响应
- **扩展性**: 适合中小型知识库（< 1000 文档）
- **准确性**: 基于关键词匹配，适合大多数场景

这个智能知识库系统让AI助手真正具备了"记忆"能力，能够基于您上传的文档提供更准确、更相关的回答！🚀
